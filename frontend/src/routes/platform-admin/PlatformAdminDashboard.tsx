import { Layout } from '../../components/Layout'

export function PlatformAdminDashboard() {
  return (
    <Layout title="Platform Admin">
      <p className="text-gray-400">
        Tenant onboarding and platform health monitoring is coming in Phase 6 (Commercialization). For now,
        tenants can be provisioned via the API directly.
      </p>
    </Layout>
  )
}
